<?php
// dev-config.php.example — copy to dev-config.php and fill values.
// KEEP dev-config.php OUT OF VERSION CONTROL (add to .gitignore).

// Example local DB credentials
if (!defined('DB_HOST')) define('DB_HOST', 'localhost');
if (!defined('DB_NAME')) define('DB_NAME', 'ankurinfotechcom_admin');
if (!defined('DB_USER')) define('DB_USER', 'ankurinfotechcom_admin');
// IMPORTANT: set your real DB password here for local development
if (!defined('DB_PASS')) define('DB_PASS', 'your_local_db_password');

// You can also override SITE_URL, UPLOAD_DIR, etc. here for development.
