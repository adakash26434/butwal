# Dev / local config (contains real DB password — never commit)
includes/dev-config.php

# PHP error logs
error_log
**/error_log
*.log

# SQLite dev database
*.sqlite
*.sqlite3
*.db

# Cache
.cache/
*.cache

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# UPLOADS & STORAGE (user-generated content — never commit, symlink instead)
uploads/*
!uploads/.gitkeep

assets/images/uploads/*
!assets/images/uploads/.gitkeep

# cPanel & hosting specific
public_html/
cpanel.yml
.cPanel.yml

# Node modules (if using any frontend build tools)
node_modules/
package-lock.json
yarn.lock

# Backup files
*.bak
*.backup
*.tmp
~*
*.swp
*.swo
